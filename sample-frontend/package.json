{
  "name": "sample-frontend",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc --noEmit && vite build",
    "preview": "vite preview",
    "generate-api": "openapi-generator-cli generate -i ../apispec/reference/todo-app.yaml -g typescript-fetch -o src/api -c openapi-generator.json"
  },
  "dependencies": {
    "vue": "^3.2.25"
  },
  "devDependencies": {
    "@openapitools/openapi-generator-cli": "^2.5.1",
    "@vitejs/plugin-vue": "^2.3.3",
    "typescript": "^4.5.4",
    "vite": "^2.9.9",
    "vue-tsc": "^0.34.7"
  }
}
